; Scopes pytest to the tests tree. Without this, pytest 8 collects the
; repository ROOT as a Package (the Blender addon __init__.py imports bpy)
; and every unit test errors during setup outside Blender.
[pytest]
