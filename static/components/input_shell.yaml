input_shell:
  class: input_shell

  input_shell_label_row:
    class: input_shell_label_row

    input_shell_icon:
      class: input_shell_icon
      img: "{{input_icon, 'user'}}"
      passive: true

    input_shell_label:
      class: input_shell_label
      text: "{{input_label, 'Prompt'}}"
      font: "{{input_label_font, 'NeueMontreal-Bold'}}"
      passive: true

  input_shell_box:
    class: input_shell_box
    data: "<INPUT>|{{input_placeholder, 'Type here...'}}"
    font: "{{input_font, 'NeueMontreal-Regular'}}"

  input_shell_footer:
    class: input_shell_footer
    text: "{{input_footer, 'Interactive input is part of the same UI surface.'}}"
    font: "{{input_footer_font, 'NeueMontreal-Italic'}}"
    passive: true